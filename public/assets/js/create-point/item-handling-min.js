const itemsToCollect=document.querySelectorAll(".items-grid li");for(item of itemsToCollect)item.addEventListener("click",handleSelectedItem);const collectedItems=document.querySelector("[name=items]");let seletctedItems=[];function handleSelectedItem(e){const t=e.target;t.classList.toggle("selected");const l=t.dataset.id;if(-1!=selectedItems.findIndex(e=>e==l)){const e=seletctedItems.filter(e=>e!=l);seletctedItems=e}else seletctedItems.push(l);collectedItems.value=seletctedItems}